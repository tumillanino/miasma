[Unit]
Description=Set default hostname if not configured
DefaultDependencies=no
Before=sysinit.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecCondition=/bin/bash -c '[ ! -s /etc/hostname ]'
ExecStart=hostnamectl hostname miasma

[Install]
WantedBy=sysinit.target
